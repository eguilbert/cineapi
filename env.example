# 🎬 TMDB / OMDB API KEYS (si tu fais du scraping ou enrichissement)
TMDB_API_KEY=your_tmdb_api_key_here
OMDB_API_KEY=your_omdb_api_key_here

# ✅ Prisma – Connexion à la base PostgreSQL (pour prisma generate, studio, etc.)
DATABASE_URL=postgresql://postgres:your_password@your-db-host.supabase.co:5432/postgres

# ✅ Supabase – Pour utiliser `createClient()` dans ton serveur Node/Nuxt
# ❗️ DOIT être en https://, PAS en postgresql://
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_public_anon_key_here

# ✅ Supabase – Clé admin pour actions sensibles côté serveur (ex: insert via RLS)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# 🔐 Si tu utilises les JWT Supabase manuellement (optionnel)
SUPABASE_JWT_SECRET=your_jwt_secret_if_needed
